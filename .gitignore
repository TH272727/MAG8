# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# runtime data
db/

# env
.env*
!.env.example

# original packaged skills stay in place, untracked (unpacked copies are committed under .claude/skills/)
*.skill

# misc
.DS_Store
npm-debug.log*

# scratch: chunk-feed upload staging (~1.6 GB of remuxed mp4s, owner-deletable)
.uploads-tmp/

# scratch: throwaway probe scripts (the `scripts/__*` convention — write, run, delete)
scripts/__*
